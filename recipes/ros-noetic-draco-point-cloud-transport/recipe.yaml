package:
  name: ros-noetic-draco-point-cloud-transport
  version: 1.0.5
source:
  git_url: https://gitlab.fel.cvut.cz/cras/ros-release/point_cloud_transport_plugins.git
  git_rev: release/noetic/draco_point_cloud_transport/1.0.5-1
  folder: ros-noetic-draco-point-cloud-transport/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 16
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - sel(linux64): sysroot_linux-64 2.17
    - ninja
    - setuptools
    - sel(unix): make
    - sel(unix): coreutils
    - sel(osx): tapi
    - sel(build_platform != target_platform): pkg-config
    - cmake
    - cython
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): numpy
  host:
    - draco
    - numpy
    - pip
    - sel(build_platform == target_platform): pkg-config
    - python
    - ros-distro-mutex 0.4 noetic
    - ros-noetic-catkin
    - ros-noetic-cras-cpp-common
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-message-generation
    - ros-noetic-pluginlib
    - ros-noetic-point-cloud-transport
    - ros-noetic-roslint
    - ros-noetic-sensor-msgs
    - ros-noetic-std-msgs
  run:
    - draco
    - python
    - ros-distro-mutex 0.4 noetic
    - ros-noetic-cras-cpp-common
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-message-runtime
    - ros-noetic-pluginlib
    - ros-noetic-point-cloud-transport
    - ros-noetic-sensor-msgs
    - ros-noetic-std-msgs
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
